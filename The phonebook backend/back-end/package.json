{
  "name": "the-phonebook-backend",
  "version": "0.0.1",
  "description": "Backend for the phonebook app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -Recurse -Force dist ; cd ../front-end ; npm run build ; Copy-Item dist -Recurse ../back-end",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui ; npm run deploy",
    "logs:prod": "fly logs"
  },
  "author": "mac-dev1",
  "license": "UNLICENSED",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.8",
    "nodemon": "^3.1.4"
  }
}
